# 物理分区与文本解题上限实施计划

用户已确认上一轮分析中的方向；在main执行，不启动子Agent，不自动commit/push。采用writing-plans与测试驱动开发流程。

目标：将原答转写、文本解题和服务延迟分开验证，准备可冻结、可追溯的小实验；真实模型调用只在用户服务器发生。

依据：根目录 `docs/content-context-ab-results-analysis-20260907.md`。48次中20超时、3格式失败；A/B原答同为5/24确认正确。新实验不改变生产接口、CV阈值、保存流程或最终95%/10%/30秒门槛。

## 1. 诊断记录

文件：新增scripts/experiment_telemetry.py，修改scripts/benchmark_content_oracle.py，新增对应测试。

- [x] 先模拟连接/读取超时和含凭据的异常，确认测试失败。
- [x] 每个请求保存UTC起止、单调时钟耗时、底层异常类型；保留服务返回的请求标识，记录实际运行源码哈希。
- [x] 只增强诊断实例，不改app/，不改变重试与20秒配置。不保存认证头或异常字符串。
- [x] 模拟网络验证每个输入仍只有一次HTTP尝试，失败仍保存分母。

## 2. 4题B/C输入

文件：新增scripts/content_partition_experiment.py、content_partition_config.json、content_partition_regions.json及测试；复用原content_context模型和服务器runner。

- [x] B复用原冻结B的JPEG与Prompt，逐字节核验；C只改变物理呈现和相应位置说明。
- [x] 从原请求图裁出题面与逐格原答；为分离后失去相邻关系的格保留对应题面图块。运题保留两行句子背景与独立目标格。格外订正不进入C。
- [x] 保留原色和字迹，不去红、不二值化；新增位置是人工诊断标注，不冒充自动CV输出。
- [x] 4题×2组×3轮=24次；同轮交替，第二轮反序。B/C输出均为transcription_v2；答案真值不进入Prompt。
- [x] 本地逐图视觉核对，运行RapidOCR旁证并保留每个小块识别结果；OCR不自动改参考答案。
- [x] 准备独立的6次服务烟测包，与正式24次分开。

## 3. 6题文本解题

文件：新增scripts/benchmark_text_oracle.py、text_oracle_config.json、text_oracle_prompt.md及测试。

- [x] 使用6题已核对的题面与原始错答，排除满足/神奇两个参考仍有歧义的样本。
- [x] 句子题明确目标格对应的原题面拼音，不能只给全句和s1却不说明目标位置；正确答案与参考解析不进入请求。
- [x] 沿用服务器LLM_API_BASE/LLM_MODEL/LLM_API_KEY配置，单独的诊断HTTP请求不复用生产函数的三次重试。
- [x] 六题三轮18次，严格Schema/ID，保存raw、模型名、usage、失败与人工评审表；不把文本上限当端到端效果。

## 4. 验证和交接

- [x] 新旧相关测试、四题实际图像、OCR证据、冻结图片/Prompt/源码哈希和压缩包往返验证。
- [x] 更新服务器命令和本地协作记录；输出烟测6次、视觉24次、文本18次三个独立目录。
- [x] 用户已确认本次16文件及message、不创建tag，仅提交；git push和服务器测试由用户执行。

停止条件：服务烟测异常先排环境；C每轮4/4完整转写才扩到28题，失败不继续盲调；文本每轮6/6答案/解析正确才考虑组合。小样本通过仅用于决定下一步，不证明总体95%。

## 执行记录

2026-09-07：前三项本地实现与模拟验证完成，61项相关测试通过。已生成四题B/C、旧冬瓜服务烟测与六题文本输入；19块OCR检测路径旁证、14块直接识别旁证及7原答格同阈值检测对照已保存。直接识别3个有字格与4个空白格输出符合参考，但qiū丢声调；全部位置为人工诊断辅助，未接入生产。

2026-09-07交接确认：用户已授权本次16文件提交，message为 `test: add partition and text oracle diagnostics`，不创建tag。完成本地提交后，由用户push并在服务器执行6/24/18三项实验。不存在真实模型结果或最终指标通过结论。
