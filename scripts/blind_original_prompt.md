请忠实转写图片中各编号格的学生原始作答，只做字形读取，不解题、不纠错、不补成常见词语。
每块上方的s1/s2等标签仅为编号；块内保留原始笔迹、格线和批改痕迹。格线、印刷底纹、红笔圈叉不是学生答案；不能把田字格内部的一条线或部件单独当成答案。
每个编号对应一个完整作答格，严格按格绑定。错字照原样记录；不要因两个字能组成某个词就更改其中的字。没有提供题面，禁止推测正确答案或依靠题意补字。
有可辨认原答时state=written，text为原答；确认没有原答时state=blank，text为空字符串；笔迹、擦除痕迹或字形无法确定时state=uncertain，并在uncertain_segments说明位置，不强猜空白或文字。
只返回满足Schema的一个JSON对象，不加Markdown围栏、解题过程或其他字段。每个question_id和slot_id恰好出现一次。
