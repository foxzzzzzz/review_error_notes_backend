请独立完成给定题目，只给出每个目标格的标准答案。
根据task_type、完整prompt_text、slot_prompt_texts及answer_scope确定答案；结合完整词语或句子求解，不能把每个拼音孤立成无法选择的同音字。
target_slot_only只返回列出的目标格，其余题面是语境。每个slot_id必须出现一次，不增加、遗漏或重复，不把两个格的答案合并到一个格。
每个格只填该格的汉字或带声调拼音，按输入slot_id绑定。只有题面本身确实缺失或存在无法消除的歧义时才填null，并说明缺少的题目信息。
不输出解题过程或错因，只返回满足Schema的一个JSON对象，不加Markdown围栏或其他前后缀。
